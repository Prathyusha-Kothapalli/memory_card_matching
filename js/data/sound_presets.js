/**
 * Memory Match Arena - Web Audio Synthesizer Presets & Sound Tables
 */
export const SOUND_PRESETS = {
  frequencies: [261.63, 293.66, 329.63, 349.23, 392.0, 440.0, 493.88, 523.25, 587.33, 659.25, 698.46, 783.99, 880.0, 987.77, 1046.5],
  chords: [
    [261.63, 327.04, 392.44],
    [293.66, 367.08, 440.49],
    [329.63, 412.04, 494.44],
    [349.23, 436.54, 523.85],
    [392.00, 490.00, 588.00],
    [440.00, 550.00, 660.00],
    [493.88, 617.35, 740.82],
    [523.25, 654.06, 784.88],
    [587.33, 734.16, 881.00],
    [659.25, 824.06, 988.88],
    [698.46, 873.08, 1047.69],
    [783.99, 979.99, 1175.99],
    [880.00, 1100.00, 1320.00],
    [987.77, 1234.71, 1481.65],
    [1046.50, 1308.12, 1569.75],
    [261.63, 327.04, 392.44],
    [293.66, 367.08, 440.49],
    [329.63, 412.04, 494.44],
    [349.23, 436.54, 523.85],
    [392.00, 490.00, 588.00],
    [440.00, 550.00, 660.00],
    [493.88, 617.35, 740.82],
    [523.25, 654.06, 784.88],
    [587.33, 734.16, 881.00],
    [659.25, 824.06, 988.88],
    [698.46, 873.08, 1047.69],
    [783.99, 979.99, 1175.99],
    [880.00, 1100.00, 1320.00],
    [987.77, 1234.71, 1481.65],
    [1046.50, 1308.12, 1569.75],
    [261.63, 327.04, 392.44],
    [293.66, 367.08, 440.49],
    [329.63, 412.04, 494.44],
    [349.23, 436.54, 523.85],
    [392.00, 490.00, 588.00],
    [440.00, 550.00, 660.00],
    [493.88, 617.35, 740.82],
    [523.25, 654.06, 784.88],
    [587.33, 734.16, 881.00],
    [659.25, 824.06, 988.88],
    [698.46, 873.08, 1047.69],
    [783.99, 979.99, 1175.99],
    [880.00, 1100.00, 1320.00],
    [987.77, 1234.71, 1481.65],
    [1046.50, 1308.12, 1569.75],
    [261.63, 327.04, 392.44],
    [293.66, 367.08, 440.49],
    [329.63, 412.04, 494.44],
    [349.23, 436.54, 523.85],
    [392.00, 490.00, 588.00]
  ],
  effects: {
    effect_1: { freq: 320, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_2: { freq: 340, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_3: { freq: 360, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_4: { freq: 380, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_5: { freq: 400, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_6: { freq: 420, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_7: { freq: 440, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_8: { freq: 460, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_9: { freq: 480, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_10: { freq: 500, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_11: { freq: 520, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_12: { freq: 540, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_13: { freq: 560, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_14: { freq: 580, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_15: { freq: 600, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_16: { freq: 620, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_17: { freq: 640, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_18: { freq: 660, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_19: { freq: 680, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_20: { freq: 700, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_21: { freq: 720, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_22: { freq: 740, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_23: { freq: 760, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_24: { freq: 780, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_25: { freq: 800, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_26: { freq: 820, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_27: { freq: 840, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_28: { freq: 860, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 },
    effect_29: { freq: 880, type: 'sine', duration: 0.15, attack: 0.01, decay: 0.14 }
  }
};

export function getSoundFrequencyPreset1(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 3) % freqs.length;
  return freqs[pos] * 1.10;
}

export function getSoundFrequencyPreset2(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 6) % freqs.length;
  return freqs[pos] * 1.20;
}

export function getSoundFrequencyPreset3(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 9) % freqs.length;
  return freqs[pos] * 1.30;
}

export function getSoundFrequencyPreset4(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 12) % freqs.length;
  return freqs[pos] * 1.40;
}

export function getSoundFrequencyPreset5(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 15) % freqs.length;
  return freqs[pos] * 1.50;
}

export function getSoundFrequencyPreset6(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 18) % freqs.length;
  return freqs[pos] * 1.60;
}

export function getSoundFrequencyPreset7(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 21) % freqs.length;
  return freqs[pos] * 1.70;
}

export function getSoundFrequencyPreset8(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 24) % freqs.length;
  return freqs[pos] * 1.80;
}

export function getSoundFrequencyPreset9(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 27) % freqs.length;
  return freqs[pos] * 1.90;
}

export function getSoundFrequencyPreset10(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 30) % freqs.length;
  return freqs[pos] * 2.00;
}

export function getSoundFrequencyPreset11(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 33) % freqs.length;
  return freqs[pos] * 2.10;
}

export function getSoundFrequencyPreset12(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 36) % freqs.length;
  return freqs[pos] * 2.20;
}

export function getSoundFrequencyPreset13(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 39) % freqs.length;
  return freqs[pos] * 2.30;
}

export function getSoundFrequencyPreset14(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 42) % freqs.length;
  return freqs[pos] * 2.40;
}

export function getSoundFrequencyPreset15(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 45) % freqs.length;
  return freqs[pos] * 2.50;
}

export function getSoundFrequencyPreset16(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 48) % freqs.length;
  return freqs[pos] * 2.60;
}

export function getSoundFrequencyPreset17(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 51) % freqs.length;
  return freqs[pos] * 2.70;
}

export function getSoundFrequencyPreset18(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 54) % freqs.length;
  return freqs[pos] * 2.80;
}

export function getSoundFrequencyPreset19(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 57) % freqs.length;
  return freqs[pos] * 2.90;
}

export function getSoundFrequencyPreset20(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 60) % freqs.length;
  return freqs[pos] * 3.00;
}

export function getSoundFrequencyPreset21(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 63) % freqs.length;
  return freqs[pos] * 3.10;
}

export function getSoundFrequencyPreset22(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 66) % freqs.length;
  return freqs[pos] * 3.20;
}

export function getSoundFrequencyPreset23(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 69) % freqs.length;
  return freqs[pos] * 3.30;
}

export function getSoundFrequencyPreset24(index) {
  const freqs = SOUND_PRESETS.frequencies;
  const pos = (index + 72) % freqs.length;
  return freqs[pos] * 3.40;
}
